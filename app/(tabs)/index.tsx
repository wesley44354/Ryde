import { SafeAreaView, Text, View } from "react-native";

export default function TabOneScreen() {
  return (
    <SafeAreaView className="flex-1 items-center justify-center bg-red-500">
      <Text className="bg-white">Tab One</Text>
    </SafeAreaView>
  );
}
